name: React Vite CI
on:
  push:
    branches: ['*']
    paths-ignore:
      - 'README.md'
      - 'docs/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - 'README.md'
      - 'docs/**'
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  all-cli-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Set environment variables
        run: mv apps/react-vite/.env.example apps/react-vite/.env
      - name: Setup pnpm
        run: corepack enable
      - name: Install dependencies (Root)
        run: pnpm install --frozen-lockfile
      - name: Build application
        run: pnpm --filter bulletproof-react-vite run build
      - name: Run tests
        run: pnpm --filter bulletproof-react-vite run test
      - name: Run linter
        run: pnpm --filter bulletproof-react-vite run lint
      - name: Check types
        run: pnpm --filter bulletproof-react-vite run check-types
  e2e:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Set environment variables
        run: mv apps/react-vite/.env.example-e2e apps/react-vite/.env
      - name: Setup pnpm
        run: corepack enable
      - name: Install dependencies (Root)
        run: pnpm install --frozen-lockfile
      - name: Install Playwright Browsers
        run: pnpm --filter bulletproof-react-vite exec playwright install --with-deps
      - name: Run Playwright tests
        run: pnpm --filter bulletproof-react-vite run test-e2e
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: |
            apps/react-vite/playwright-report/
            apps/react-vite/mocked-db.json
          retention-days: 30
